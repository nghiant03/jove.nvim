"""jove_bridge — stdio JSON-lines Jupyter kernel bridge for jove.nvim.

Run with ``python -m jove_bridge``. The wire contract lives in the repo's
PROTOCOL.md; the pytest suite in ``tests/`` is the executable form of it.
"""

__version__ = "0.1.0"
